import { Link } from 'react-router-dom';

import styles from './Card.module.scss';
import classNames from 'classnames/bind';
const cx = classNames.bind(styles);

function Card({ data, right = false, className = '' }) {
  return <div>h1</div>;
}

export default Card;
